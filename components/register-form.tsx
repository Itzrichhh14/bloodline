"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { Card } from "@/components/ui";
export function RegisterForm({discordId,username}:{discordId:string;username:string}){const [code,setCode]=useState("");const [roblox,setRoblox]=useState("");const [busy,setBusy]=useState(false);const [err,setErr]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setErr("");const r=await fetch("/api/invitations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({code,robloxName:roblox,discordId,username})});const d=await r.json();if(!r.ok){setErr(d.error||"Could not register");setBusy(false);return}router.push("/profile");router.refresh()}return <Card className="w-full max-w-md p-8"><p className="text-sm font-bold text-[#ef3340]">WELCOME</p><h1 className="mt-2 text-3xl font-black">Join Bloodline</h1><p className="mt-3 text-sm text-zinc-400">Discord: @{username}</p><form onSubmit={submit} className="mt-7 space-y-4"><input required value={code} onChange={e=>setCode(e.target.value)} placeholder="Invitation code" className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-[#ef3340]"/><input value={roblox} onChange={e=>setRoblox(e.target.value)} placeholder="Roblox username (optional)" className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-[#ef3340]"/>{err&&<p className="text-sm text-red-400">{err}</p>}<button disabled={busy} className="w-full rounded-xl bg-[#ef3340] px-5 py-3 font-bold disabled:opacity-50">{busy?"Creating…":"Create member account"}</button></form></Card>}
