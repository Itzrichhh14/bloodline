import { signIn } from "@/lib/auth"; import { Button,Card } from "@/components/ui";
export default function Login(){return <main className="grid min-h-[calc(100vh-64px)] place-items-center px-5"><Card className="w-full max-w-md p-8"><p className="text-sm font-bold text-[#ef3340]">MEMBER AREA</p><h1 className="mt-2 text-3xl font-black">Sign in</h1><p className="mt-3 text-sm leading-6 text-zinc-400">Use Discord to access your Bloodline account.</p><form action={async()=>{ "use server"; await signIn("discord",{redirectTo:"/profile"});}} className="mt-7"><Button>Continue with Discord</Button></form><p className="mt-5 text-xs text-zinc-500">New members need an invitation code during registration.</p></Card></main>}
