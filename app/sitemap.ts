import type { MetadataRoute } from "next"; import { db } from "@/lib/db"; export const dynamic="force-dynamic"; export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";const [news,members]=await Promise.all([db.news.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}}),db.user.findMany({where:{active:true},select:{id:true,updatedAt:true}})]);return [{url:base,priority:1},{url:`${base}/about`,priority:.7},{url:`${base}/members`,priority:.8},{url:`${base}/rankings`,priority:.9},{url:`${base}/news`,priority:.8},...members.map(member=>({url:`${base}/members/${member.id}`,lastModified:member.updatedAt,priority:.5})),...news.map(n=>({url:`${base}/news/${n.slug}`,lastModified:n.updatedAt,priority:.7}))]}
